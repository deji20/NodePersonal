//functional approach to iterating through arrays

const failures = [
    "mislick", "falling down", "skateboard accident", "tiktok fail", 
    "running into an old friend, and having nothing to say", "my life"];

failures.map((fail) => {
    console.log(fail);
    return fail;
})